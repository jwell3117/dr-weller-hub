Replace both `index.html` and `chart-builder.html` in the repository with the files in this folder. Commit and push them together, wait for Vercel to deploy, then hard-refresh the live page with Ctrl+Shift+R.

Complete ICD-10 codes are generated when the selected details support them. When additional specificity is needed, the closest reliable ICD-10 family is displayed so it can be pasted into the EHR search and refined there. Verify the final diagnosis and code in the EHR before submission.
